export const DATE_FORMAT: string = 'dd/MM/yyyy';
export const DATE_TIME_FORMAT: string = 'dd/MM/yyyy HH:mm';
export const TIME_FORMAT: string = 'HH:mm';

export class Constants{
  public static readonly DATE_FORMAT: string = DATE_FORMAT;
  public static readonly DATE_TIME_FORMAT: string = DATE_TIME_FORMAT;
  public static readonly TIME_FORMAT: string = TIME_FORMAT;
}